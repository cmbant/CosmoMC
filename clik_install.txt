
 ./waf configure --install_all_deps --lapack_mkl=${MKLROOT} --lapack_mkl_version=10.3 --no_bopix


set the following in ~/.bashrc (for your equivalent of /scratch/aml1005)

export PLANCKLIKE=cliklike
export CLIKPATH=/scratch/aml1005/clik_10.0/clik-10.0b1
export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:$CLIKPATH/lib

Log out and in again.

Make symlink from /scratch/aml1005 to ./data/clik:

ln -s /scratch/aml1005/clik_10.0 data/clik
